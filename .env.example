# GroceSplit Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# FIREBASE CONFIGURATION (Required for cloud sync)
# ===========================================
# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project or select existing
# 3. Go to Project Settings > General > Your apps
# 4. Add a web app and copy the config values

FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# ===========================================
# FIRESTORE SETUP
# ===========================================
# After setting up Firebase:
# 1. Go to Firestore Database in Firebase Console
# 2. Create database (start in test mode for development)
# 3. The app will automatically create 'items' and 'users' collections

# ===========================================
# GEMINI AI CONFIGURATION (Optional - for AI features)
# ===========================================
# Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# CONVERTING TO APK (Android App)
# ===========================================
# Option 1: PWA Builder (Recommended)
# 1. Build the app: npm run build
# 2. Deploy to a hosting service (Firebase Hosting, Vercel, Netlify)
# 3. Go to https://www.pwabuilder.com/
# 4. Enter your deployed URL
# 5. Download the Android package

# Option 2: Capacitor
# 1. npm install @capacitor/core @capacitor/cli @capacitor/android
# 2. npx cap init GroceSplit com.grocesplit.app
# 3. npm run build
# 4. npx cap add android
# 5. npx cap sync
# 6. npx cap open android (opens Android Studio)
# 7. Build APK from Android Studio
